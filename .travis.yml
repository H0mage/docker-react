language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t h0mage/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true h0mage/docker-react npm run test